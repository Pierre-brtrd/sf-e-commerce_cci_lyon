{% extends 'base.html.twig' %}

{% block title %}
	Validation commande: adresse
	{{ parent() }}
{% endblock %}

{% block body %}
	<section class="container mt-2">
		<h1 class="text-center">Validation de commande: adresse</h1>
		<a href="{{ path('app.cart.index') }}" class="btn btn-primary my-3">Retour au panier</a>
		<div class="row">
			<div class="col-md-8">
				<twig:CheckoutAddressForm form="{{ form }}" addresses="{{ app.user.addresses }}"/>
			</div>
			<div class="col-md-4">
				<twig:CartTotal cart="{{ cart }}"/>
			</div>
		</div>
	</section>
{% endblock %}
